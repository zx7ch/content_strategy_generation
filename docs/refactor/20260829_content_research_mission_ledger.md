# Content Research Refactor Mission Ledger

| Field | Content |
| --- | --- |
| Mission | 按已批准计划补齐启动动作并重构 Content Research，同时保证所有功能和 Task 3.1 当前发布 ZIP 的能力完全一致。 |
| Current slice | SQLite R7 implementation、live verification、clean-commit candidate packaging and release gates complete。 |
| Contract IDs | `STATE-SQL-06`, `STATE-SQL-10`, `STATE-SQL-11`, `AUTH-SQL-12`, `AUTH-SQL-13`, `INV-SQL-08`, `INV-SQL-11`, `INV-SQL-12`, `FAIL-SQL-08`, `FAIL-SQL-15`, `FAIL-SQL-16`, `ACC-SQL-11`, `ACC-SQL-12`, `ACC-SQL-13`, `ACC-SQL-14`, `ACC-SQL-15`, `ACC-SQL-16`，以及全部既有回归。 |
| Acceptance RED | All R7 seams are green：`RuntimeWriteCoordinator.submit`/typed mutation、`SingleWriterDataMigrator.inspect/migrate`、`/health`、canonical Creator API/browser。`test_trace_snapshot_causal_revision_and_trigger_cutover` proves zero Trace triggers；one successful Run mutation advances revision exactly once；replay/conflict/rollback/global mutation advances zero；long polling requires the exact causal revision。 |
| Last green proof | Full backend `1436 passed, 16 skipped` with no exclusions；R7 Writer/migration/lifecycle/scheduler focused gate `46 passed, 2 skipped`；Creator frontend `83 passed` + TypeScript；production frontend build green；owned-stack Chromium Browser `9 passed` plus focused analysis-recovery Browser `1 passed`。Real OpenAI + XHS canary `run_65affa50c9ae24751c5c5a47ea91f28f` reached `report_ready` at lifecycle revision 11 after a failed-analysis successor retry without recollection：36 discovered、32 deduped、6 admitted sources、66 frozen citations/findings、1 partial verified publication；reload restored the same report and Trace。SQLite `quick_check=ok`、Trace triggers `0`、Writer-owned Trace revision `130`。Clean-source Runtime ZIP gate `4 passed`：archive safety and exact source identity、bounded cold-start request jitter、frontend/Runtime contract pairing、frozen Runtime two-restart persistence all green；matching production frontend build green。 |
| Finding route | R7 live logs found an `IMPLEMENTATION_DEFECT`：analysis failure first committed canonical `recovery_required`, then the transitional workflow runtime attempted the same recovery transition and raised `waiting_user` conflict。A red integration seam reproduced the exact duplicate write；the second authority was deleted and recovery regression is `14 passed`。Full-suite failures were then traced to stale `app.state` dependencies surviving `app.main` shutdown plus API unit tests depending on import order；lifespan now removes installed runtime dependencies and injected unit APIs explicitly suppress production startup。Discarded endpoint/in-memory/env/extension request-shape tests were removed or rewritten to current contracts。The consolidated release gate exposed a test-infrastructure flake：the 1-second health-probe timeout was also applied to post-health Trace/report capability reads；timing probes showed normal Trace reads at 10–14ms but background cold-start scheduling could exhaust that unrelated probe budget。Health polling remains 1 second per attempt while bounded capability reads now allow 5 seconds；a deterministic jitter regression is green。 |
| Return point | R7 code、migration、full regression、live browser、candidate artifact and exact SHA/frontend pairing are green。 |
| Next action | Handoff the single clean R7 commit and its matching Runtime ZIP for review/merge；do not rebuild from a different or dirty source tree。 |
| Open risk | The 16 skipped tests require external acceptance dependencies and are not regressions；two `websockets` deprecation warnings and existing non-blocking frontend lint warnings remain。 |
